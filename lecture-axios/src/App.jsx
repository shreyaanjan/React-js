import { useEffect } from "react";

const App = () => {
    const url = "http://localhost:8217/users";

    useEffect(()=>{
        const fetchUsers = async () => {
            await axious.get
        }

        fetchUsers();
    }, [])
    return (
        <div>

        </div>
    )
}

export default App